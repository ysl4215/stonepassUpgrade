package com.swempire.web.comm.util;

import java.util.List;

import javax.inject.Inject;

import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

import com.swempire.web.condition.Curl2;
import com.swempire.web.condition.VO.ConditionVO;
import com.swempire.web.condition.service.ConditionService;

@Component
public class Scheduler{

	
	@Inject
	ConditionService conditionservice;
	
	Curl2 curl = new Curl2();
	
	
	  @Scheduled(fixedDelay = 3000) public boolean autoUpdate() throws Exception {
	  
		 List<ConditionVO> list= conditionservice.getBoardList();
		  
		  System.out.println(conditionservice.getBoardList().get().getOrga_url());
		  
		  for(int i=0; i<5; i++) {
			  
		  }
		  
		  String url = conditionservice.getBoardList().get(0).getOrga_url();
		  curl.get(url, null);
		  
	  
	return false;
	  
	  }
	 
	
	

	
}
